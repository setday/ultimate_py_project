#pragma once

namespace unreal_fluid::compositor {
  class Compositor {

  public:
    Compositor();
    ~Compositor();

    void init();
    void destroy();
  }; // compositor class
} // namespace unreal_fluid::compositor
